PORT=3780
TZ=Asia/Jerusalem
JWT_SECRET=replace-with-a-long-random-string

# Closed demo gate — never commit real values
GATE_USER=liad
GATE_PASSWORD=
GATE_PATH=

# Local development only (skips Basic Auth + secret path)
# GATE_DISABLED=1
